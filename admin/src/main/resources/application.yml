spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project-blog
    username: admin
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update

jwt:
  private.key: classpath:app.key